import {ColumnHolder} from '@/app/components/Section/column-holder';
import {SectionHolder} from '@/app/components/Section/section-holder';

export interface SectionViewModel {
    Columns: ColumnHolder[];
    Section: SectionHolder;
}

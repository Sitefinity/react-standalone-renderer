'use client';

import React from 'react';
import styles from '@/app/components/ClientSide/ClientSide.module.css';

type Props = {};

const ClientSide = (props: Props) => {
    return <div className={styles.container}>ClientSide</div>;
};

export default ClientSide;

import React from 'react';
import styles from '@/app/components/ServerSide/ServerSide.module.css';

type Props = {};

const ServerSide = (props: Props) => {
    return <div className={styles.container}>ServerSide</div>;
};

export default ServerSide;
